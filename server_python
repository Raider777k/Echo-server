# простейший однопоточный сервер 
#! /usr/bin/env python
import socket # импортирует стандартную билиотеку сокетов

sock = socket.socket() # создание объекта сокета
sock.bind(('', 7070)) # bind связывает объект сокета с определенным ip и портом и как раз делает сокет серверным
sock.listen(1) # запуск прослушивания и в данном случае ождидается только 1 клиент (все процессы приостанавливаются и включается режим Хатико)
con, add = sock.accept() # в conn записывается используемый сокет, а в add адрес клиента, accept - приняте запроса от клиента

print("Вот он! Адрес клиента: ", add) # комментарии излишни 

Whike True:
  data = conn.recv(1024) # получаем от клиента данные объемом 1 Кбайт
  if not data:
    break
  conn.send(data.upper()) # возвращаем клиенту данные с изменениями (в данном случае мы просто поменяли регист у запроса)
 
conn.close() # завершение сессии и выключение сокета

________________________________________________________________________________________________
# простейший клиент
#! /usr/bin/env python

import socet

sock = socket.socket()
sock.connect(('localhost', 7070)) # connect создает клиентский сокет и пытается соединиться с сервером
sock.send('Привет, Андрй! Ну, где ты был, Ну ... ')

data = sock.recv(1024)
sock.close()

print(data)
